# bool
- 원래는 0값은 거짓, 0을 제외한 나머지값을 참으로 간주했다.
- 현재 C++은 True, False bool 자료형을 지원한다.
- 옛 버전과의 호환성을 위해, true==1, false==0으로 평가되어, bool은 int 타입과 서로 호환된다.
- boolalpha가 있어야만 bool값을 true or false로 출력한다.
- 일반적인 경우, 1 또는 0 으로 출력된다.

# 논리 연산자
- x && y : AND 연산
- x || y : or 연산
- !x : NOT 연산



